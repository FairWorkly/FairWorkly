import { PublicLayout } from './PublicLayout'

/**
 * Marketing pages layout (HomePage, Templates, etc.)
 * Uses center variant - full-width container for rich content
 */
export function PublicMarketingLayout() {
  return <PublicLayout variant="center" />
}
