/**
 * “特效背景”，就是那份 HTML 原型里登录页那种 “不是纯色背景，而是带一点氛围的底纹”，主要由两层组成（都很轻，不是动画）：

1) Radial glow（光晕渐变）

像两个很淡的“灯光”打在背景上

一个偏蓝紫（主色），一个偏粉（辅色）

让页面看起来更“有品牌感”、不那么扁平

它本质上就是 CSS 的：

radial-gradient(...)（径向渐变）

2) Grid background（细网格底纹）

背景上有非常淡的横竖线

像“设计网格纸”一样

让页面更“科技/产品化”，但不会抢内容

它本质上就是 CSS 的：

linear-gradient(...) 两条叠加（横线+竖线）

background-size 控制网格间距
 */
