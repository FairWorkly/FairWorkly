{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Agent Service",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "master_agent.main:app",
                "--port", "8000",
                "--reload"
            ],
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env",
            "jinja": true
        }
    ]
}
